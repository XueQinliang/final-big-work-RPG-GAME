#include "imags.h"
#include <qfont.h>
#include <iostream>
/*imags::imags(int x1, int y1, int x2, int y2, int id)
{
    x1 = x1;
    y1 = y1;
    x2 = x2;
    y2 = y2;
    id = id;
}
void imags::setAttack(int n)
{
    attack = n;
}
void imags::setBlood(int n)
{
    blood = n;
}
void imags::setDefense(int n)
{
    defense = n;
}
void imags::setMagic(int n)
{
    magic = n;
}

void imags::AttributeSetting(int id,int sign ,player &p1)
{
    switch(id)
    {
    case 1:p1.bloodchange(100*sign);break;
    case 2:p1.magicchange(100*sign);break;
    case 3:p1.defensechange(100*sign);break;
    case 4:p1.defensechange(200*sign);break;
    case 5:p1.defensechange(300*sign);break;
    case 6:p1.attackchange(100*sign);break;
    case 7:p1.attackchange(200*sign);break;
    case 8:p1.attackchange(300*sign);break;
    default:break;
    }
}
*/
